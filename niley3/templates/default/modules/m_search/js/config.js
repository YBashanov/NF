if (! M_search) {    Error.add("M_search: модуль M_search не определен");}else {    //метод работы: direct (меняет адресную строку), outer (перенаправляет параметры на иной модуль), ajax (посылает запрос на сервер)    M_search.settings.action = "outer";        //action = "outer"    //внешний метод (метод иного модуля), который сейчас получает (формирует) данные, которые мы ищем    M_search.settings.outerMetod = function(params, callback){Fotosarea.search(params, callback)};        //action = "ajax"    //путь до ajax-файла (без .php), не обязательно этого модуля.    //Это может быть тот модуль, который собственно и выдает данные, которые мы ищем    M_search.settings.path = "";        //action = "ajax"    //блок, в который надо будет вывести данные    M_search.settings.parent_div = "";            //внешний вид ----------------------------------------------        //путь до фоновой картинки прогресса    M_search.settings.pathBgImage = "templates/" + template + "/modules/m_search/image/input_search.png";        //возвращает текст в зависимости от языка    M_search.getLangText = function(key){        var object = {			"ru" : {                //пустая строка				"empty" : "",                   //при загрузке блока				"search" : "поиск...",                  //когда ничего не найдено				"nothing" : "Ничего не найдено" 			},			"en" : {                "empty" : "",				"search" : "search...",                "nothing" : "Nothing found"			}		}		return object[language][key];    }        //стили для отображения ошибки    M_search.settings.errorStyles = {color : "#f00"};}